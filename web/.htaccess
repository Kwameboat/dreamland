RewriteEngine On

# Leave /admin and /api to the Yii PHP apps
RewriteRule ^admin(/|$) - [L]
RewriteRule ^api(/|$) - [L]

# PWA client-side routes
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.html [L]
