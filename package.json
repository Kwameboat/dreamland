{
  "name": "dreamland",
  "private": true,
  "scripts": {
    "icons": "node scripts/generate-pwa-icons.js",
    "build:web": "node scripts/generate-pwa-icons.js && node scripts/generate-vercel-env.js"
  },
  "devDependencies": {
    "sharp": "^0.33.5"
  }
}
